server {
    listen 80;
    listen [::]:80;
    server_name ingres-agent.space www.ingres-agent.space;

    location / {
        proxy_pass http://127.0.0.1:5173;

        # Forward essential headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Optional: prevent large request errors
        client_max_body_size 10M;
    }
}


sudo apt install nginx -y
sudo systemctl status nginx         (checking)
sudo systemctl start nginx
sudo systemctl enable nginx
sudo nano /etc/nginx/sites-available/ingres-agent.space

paste the above code then Press Ctrl + O → Enter → Ctrl + X

sudo ln -s /etc/nginx/sites-available/ingres-agent.space /etc/nginx/sites-enabled/
sudo rm /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx



